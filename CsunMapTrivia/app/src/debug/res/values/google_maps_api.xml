<resources>
    <!--
    TODO: Before you run this application, you need a Google Maps API key.
    (debug only)
    You will need the SHA-1 fingerprint from your computer:
        (For MAC) On the terminal run the following (assuming you already have the java keytool):
            keytool -exportcert -alias androiddebugkey -keystore ~/.android/debug.keystore -list -v
        From the Certificate Fingerprints that show up get a hold of the SHA-1:
            Ex. SHA1: xx:xx:...:xx
    Go to your Google Developers Console and create a new project
    Follow the instructions.

    Once you have your key (it starts with "AIza"), replace the "AIza..."
    string in this file.
    -->
    <string name="google_maps_key" translatable="false" templateMergeStrategy="preserve">AIzaSyChD75oFP-mBTCboOS4quyzE5sD2K_f0Ak</string>
</resources>
